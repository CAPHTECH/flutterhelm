{
  "name": "@flutterhelm/harness",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bootstrap": "pnpm run build && node --enable-source-maps dist/cli.js bootstrap",
    "run": "node --enable-source-maps dist/cli.js run",
    "validate": "pnpm run build && node --enable-source-maps dist/cli.js validate",
    "report": "node --enable-source-maps dist/cli.js report",
    "smoke": "pnpm run bootstrap && pnpm run validate && pnpm run run -- --tag smoke",
    "contracts": "pnpm run build && pnpm run validate && pnpm run run -- --tag regression",
    "runtime": "pnpm run build && pnpm run validate && pnpm run run -- --tag runtime",
    "profiling": "pnpm run build && pnpm run validate && pnpm run run -- --tag profiling",
    "bridge": "pnpm run build && pnpm run validate && pnpm run run -- --tag bridge",
    "interaction": "pnpm run build && pnpm run validate && pnpm run run -- --tag interaction",
    "hardening": "pnpm run build && pnpm run validate && pnpm run run -- --tag hardening",
    "ecosystem": "pnpm run build && pnpm run validate && pnpm run run -- --tag ecosystem",
    "qa": "pnpm run build && pnpm run validate && pnpm run run -- --tag edge && pnpm run run -- --tag adversarial"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3"
  }
}
