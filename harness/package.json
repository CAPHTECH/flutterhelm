{
  "name": "@flutterhelm/harness",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bootstrap": "npm run build && node --enable-source-maps dist/cli.js bootstrap",
    "run": "node --enable-source-maps dist/cli.js run",
    "validate": "node --enable-source-maps dist/cli.js validate",
    "report": "node --enable-source-maps dist/cli.js report",
    "smoke": "npm run bootstrap && npm run validate && npm run run -- --tag smoke",
    "contracts": "npm run build && npm run validate && npm run run -- --tag regression",
    "qa": "npm run build && npm run validate && npm run run -- --tag edge && npm run run -- --tag adversarial"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.3"
  }
}
