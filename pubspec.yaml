name: flutterhelm
description: Agent-safe orchestration layer / MCP server for Flutter development.
publish_to: none
version: 0.1.0-dev.0

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.6.0
  path: ^1.9.1
  vm_service: ^15.0.2
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.2
